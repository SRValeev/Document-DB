# Additional requirements for building offline distribution
# Python 3.8-3.12 compatible versions
huggingface_hub>=0.19.0,<1.0.0
requests>=2.31.0,<3.0.0
spacy>=3.7.0,<4.0.0

# Build tools
wheel>=0.41.0,<1.0.0
setuptools>=68.0.0,<70.0.0
pip>=23.2.0,<25.0.0

# Heavy models for offline distribution
# Note: These will be downloaded during build process
# intfloat/multilingual-e5-large (2.24GB) - via huggingface_hub
# ru_core_news_lg (560MB) - via spaCy model repository

# SpaCy models are NOT PyPI packages and must be downloaded separately
# Use: python -m spacy download ru_core_news_lg
# Or download directly from: https://github.com/explosion/spacy-models/releases/